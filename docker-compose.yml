version: '3.0'

services:
  node-app:
    build: .
    container_name: covid-bot
    env_file:
      - /credentials/covid-bot/env_file
    volumes:
      - /opt/covid-bot/data/cvd.db:/opt/services/node/src/cvd.db
    restart: always
